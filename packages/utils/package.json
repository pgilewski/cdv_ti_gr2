{
  "name": "@cdv/techint-utils",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "tsconfig-techint": "*"
  }
}
