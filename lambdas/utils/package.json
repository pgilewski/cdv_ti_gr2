{
  "name": "@cdv/techint-lambdas-utils",
  "version": "0.0.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "test:watch": "jest --watch",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "dependencies": {
    "@aws-lambda-powertools/logger": "^1.8.0",
    "@aws-lambda-powertools/metrics": "^1.8.0",
    "@aws-lambda-powertools/tracer": "^1.8.0"
  }
}
