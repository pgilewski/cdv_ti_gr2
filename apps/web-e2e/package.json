{
  "name": "@siili/techint-web-e2e",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "test:e2e": "playwright test",
    "clean": "rm -rf node_modules && rm -rf playwright-report && rm -rf test-results"
  },
  "devDependencies": {
    "@playwright/test": "^1.32.3"
  }
}
